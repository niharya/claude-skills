# Examples

Add sample audit reports or before/after CSS diffs here.

Suggested formats:
- `audit-report-[project-name].md` — a Phase 1 inventory report from a real session
- `before-[filename].css` + `after-[filename].css` — a before/after diff from a full audit
- `score-card-[project-name].md` — the final score and blueprint from a completed session
