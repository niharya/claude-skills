# phrasings.md

<!-- FIRST RUN: filled by prompt 2 in your home AI. See prompts/02-voice-extraction.md.
     This file is also where the skill writes new phrasings it learns at the
     end of each application run, when the candidate confirms. -->

Verbatim language the candidate uses. The skill prefers these phrases verbatim over paraphrases. Tags: APPROVED (use freely), NEUTRAL (use if it fits), KILLED (never use).

## APPROVED — phrases the candidate has used and would use again

- <TODO: filled by setup>

## NEUTRAL — phrases the candidate is fine with but doesn't lean on

- <TODO: filled by setup>

## KILLED — phrases that sound wrong for this candidate, even if the AI generated them

- <TODO: filled by setup>
